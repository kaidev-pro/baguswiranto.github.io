import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Work — Kaidevlab",
  description: "Real products, honest status, clear direction. Explore AI business systems, developer tools, education platforms, and creative experiments by Kai.",
};

export default function WorkLayout({ children }: { children: React.ReactNode }) {
  return children;
}
